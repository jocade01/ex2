{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {% if app.user %}

        <h1>Hello {{ app.user.name }}! ✅</h1>
    <a href="/logout">Logout</a>
    {% else %}
        <a href="/register">Register</a>
        <a href="/login">Login</a>
    {%endif %}
    <a href="/session">Sesiones</a>
    <p>{{ moviet }}</p>
    {% for movie in moviee %}
    {% for movi in movie %}
    <p>{{ movi.room }}</p>
    {% endfor %}     {% endfor %}

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>C:/Users/Jose/PhpstormProjects/25-26-dwes-ex2ev-MarcosRP13-main/ex2/src/Controller/MainController.php</code></li>
        <li>Your template at <code>C:/Users/Jose/PhpstormProjects/25-26-dwes-ex2ev-MarcosRP13-main/ex2/templates/main/index.html.twig</code></li>
    </ul>

    <tr>
        <th>Poster</th>
        <th>Title</th>
        <th>Synopsis</th>
        <th>Trailer</th>
    </tr>
    {% for session in sessionsall %}
        <tr>
            <td>
                <img src="{{ session.movie.poster }}">
            </td>
            <td>
                {{ session.movie.title }}
            </td>
            <td>
                {{ session.movie.synopsis }}
            </td>
            <td>
                <a href=" {{ session.movie.trailer }}"> {{ session.movie.trailer }}</a>
            </td>
        </tr>
    {% endfor %}
</div>
{% endblock %}
